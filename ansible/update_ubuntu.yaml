---
- name: Update and Upgrade Ubuntu Dependencies
  hosts: ubuntu_server
  become: yes
  tasks:
    - name: Update Apt Package Index
      apt:
        update_cache: yes
    - name: Upgrade All Packages
      apt:
        upgrade: dist
